*.o
*.dSYM/
/sesame
/fuzz_idat
/fuzz_replay
/corpus/
compile_commands.json
normexp_test
cbs_test
mu2cg
pipeline_dump

# working notes (design + next steps), never committed
/TODO.md

# Platform data (ordering tables, masks, CNV normals) and bootstrap outputs.
# Never in git: annotation comes from zhou-lab/InfiniumAnnotation via
# `yame fetch` into the shared store, and the CNV normal panel is built
# locally with `make cnv-normals`.
/data/
/testdata/

## byte-compiled CI helpers (tools/check_glibc_floor.py)
__pycache__/
