*.o
*.dSYM/
/sesame
/fuzz_idat
/fuzz_replay
/corpus/
compile_commands.json
normexp_test
cbs_test
mu2cg
pipeline_dump

# Platform data (ordering tables, masks, CNV normals) and bootstrap outputs.
# These ship via GitHub Releases (index-<platform>-v<n> tags), never in git.
/data/
/testdata/
